server:
  port: 8080


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/esathi_db?useSSL=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.springframework.security: DEBUG



#    database-platform: org.hibernate.dialect.MySQLDialect
